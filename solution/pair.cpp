#include <vector>
#include <iostream>
#include <algorithm>


pair<int, int> arr[300];


int main () {
    ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

    
    arr[0].first;
    arr[0].second;

    memset()
}